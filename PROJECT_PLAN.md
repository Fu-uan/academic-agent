# PaperAgent 2.0 — 智能化学术研究辅助系统

> 项目规划文档 · v0.1 · 2026-07-07

---

## 一、项目概述

### 1.1 项目愿景

面向高校本硕博学生，打造一个**覆盖论文研究全流程的智能辅助系统**：从课题方向引导、文献检索与综述生成，到数据实验辅助与结果解读，一站式降低学术研究入门门槛。

### 1.2 核心价值

| 痛点 | 解决方案 |
|------|---------|
| 不知道怎么开始检索、不会提炼关键词 | 对话引导 + 智能关键词扩写 |
| 一篇篇复制引文、手动排版综述 | 自动批量检索 + LLM 综述生成 + 可视化 |
| Stata 数据清洗、回归重复劳动 | 模板化批处理 + 一键执行 |
| 跑完回归不知道结果怎么解读 | LLM 自动解读 + 创新点提炼 |

### 1.3 目标用户

- 本科生（毕业论文、大创项目）
- 硕士研究生（小论文、学位论文）
- 博士研究生（文献调研、系统综述）

### 1.4 与现有 PaperAgent v1 的关系

v1 是已开发完成的文献检索 Web 应用，作为 v2 的「模块A」底座。**以下为 v1 已有内容，非 v2 新增**：

#### v1 目录结构

```
paper-agent/
├── app.py              # FastAPI 后端（主服务）
├── requirements.txt     # 依赖
├── static/
│   └── index.html      # 前端页面（单 HTML，CSS/JS 全部内联）
└── venv/               # Python 虚拟环境
```

启动方式：`cd paper-agent && source venv/bin/activate && python -m uvicorn app:app --host 0.0.0.0 --port 8080`

#### v1 已实现功能

| 功能 | 说明 |
|------|------|
| OpenAlex API 检索 | 关键词 + 年份筛选 + 引用量排序 |
| 年份筛选 | 近五年 / 近十年 / 自定义区间 |
| 数据库按钮 | 开放学术 / 知网 / WoS / 万方 / 维普（非开放库自动引导到 OpenAlex） |
| 文献卡片展示 | 标题(可点击DOI跳转) / 作者 / 期刊 / 年份 / 引用量 / Q1-Q4分区 / 类型标签 / OA 标签 |
| 摘要展开/收起 | 点击按钮切换 |
| GB/T 7714 引文 | 单篇生成 + 一键复制 |
| 批量引文导出 | 勾选多篇 → 弹窗 → 一键复制全部 |
| 学术翻译 | 内置术语库（理工/人文社科各 80+ 条目），术语级替换翻译 |
| 关键词扩写 | 本地同义词规则库扩展 |
| 智能扩词重试 | 无结果时自动扩展关键词重新检索 |
| 无结果提示 | 显示检索优化建议 |

#### v1 API 接口

| 方法 | 路径 | 说明 | 已实现 |
|------|------|------|--------|
| POST | /api/search | 关键词检索（OpenAlex） | ✅ |
| POST | /api/cite | 生成 GB/T 7714 引文 | ✅ |
| POST | /api/translate | 学术术语翻译 | ✅ |
| POST | /api/expand | 关键词扩写 | ✅ |
| GET | /api/databases | 数据库列表 | ✅ |
| GET | /api/health | 健康检查 | ✅ |

#### v1 不包含的内容（v2 需从零开发）

- 对话式引导交互
- LLM 接入（关键词提取、综述生成、结果解读）
- 文献综述生成
- ECharts 可视化图表
- 数据上传与预览
- Stata 集成
- 排列组合遍历
- 结果表格生成与 LLM 解读
- 论文章节草稿导出

---

## 二、总体架构

### 2.1 系统架构

```
┌────────────────────────────────────────────────────┐
│                   前端 Web UI                       │
│  (HTML/CSS/JS + ECharts 可视化)                    │
│  对话检索 → 文献列表 → 综述预览 → 数据分析面板      │
└──────────────────┬─────────────────────────────────┘
                   │ HTTP API
┌──────────────────▼─────────────────────────────────┐
│               FastAPI 后端层                        │
│                                                     │
│  ┌─────────┐  ┌──────────┐  ┌───────────────────┐  │
│  │ 模块A    │  │ 模块B    │  │ 模块C             │  │
│  │ 检索引导 │  │ 综述生成 │  │ 数据分析 + 解读   │  │
│  └────┬────┘  └────┬─────┘  └────────┬──────────┘  │
│       │             │                 │              │
│  ┌────▼─────────────▼─────────────────▼──────────┐  │
│  │              LLM 接入层                       │  │
│  │  (关键词提取、综述生成、结果解读)              │  │
│  └───────────────────────────────────────────────┘  │
└──────────────────┬─────────────────────────────────┘
                   │
┌──────────────────▼─────────────────────────────────┐
│              数据源 & 工具层                        │
│  OpenAlex  │  WoS API  │ Stata  │ 本地术语库       │
└─────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层 | 技术 | 备注 |
|----|------|------|
| 前端 | 原生 HTML/CSS/JS + ECharts | 依赖 BootCDN，不引入框架 |
| 后端 | Python FastAPI + httpx | 异步，单服务部署 |
| LLM | Hermes API / 兼容 OpenAI 接口 | 可选配置，支持 GLM/DeepSeek |
| 数据源 | OpenAlex（主） + WoS API（辅） | |
| 工具 | Stata/MP（通过 PowerShell 桥接） | |
| 可视化 | ECharts 5 | 从 BootCDN 加载 |

### 2.3 数据流

```
用户输入研究方向
    ↓
对话引导 → 提炼核心关键词
    ↓
关键词优化 → 多库检索（OpenAlex + WoS）
    ↓
批量获取文献元数据
    ↓
  ┌─ 模块B: LLM分析 → 综述文本 → 可视化图表
  │
  └─ 模块C: 用户上传数据 → 模板化Stata脚本 → 回归结果 → LLM解读
```

---

## 三、模块详细设计

### 模块A：对话式智能检索

**定位**：论文研究的入口，替代"上来就搜"的盲目检索方式。

**流程**：

```
1. 系统提问「你想研究什么方向？」
2. 用户输入：企业数字化转型
3. 系统追问：
   - 关注因果效应还是测度构建？
   - 是否限定特定行业/地区？
   - 方法论偏好（DID/面板/案例）？
4. 系统输出：
   - 推荐关键词列表（主词 + 扩展词）
   - 推荐检索式（含逻辑组合）
   - 数据库选择建议
5. 用户确认 → 执行检索 → 显示结果
```

**核心功能**：

| 功能 | 说明 |
|------|------|
| 多轮对话引导 | 根据用户输入逐步细化研究方向 |
| 智能关键词提取 | 从自然语言描述中提取学术关键词 |
| 关键词扩写 | 同义词、上下位词、相关词扩展 |
| 检索式生成 | AND/OR/NOT逻辑组合 |
| 多库切换 | OpenAlex / WoS / CNKI（引导） |
| 文献列表 | 标题、作者、期刊、引用量、分区、摘要 |
| 引文导出 | GB/T 7714 格式，单篇 + 批量 |
| 摘要翻译 | 内置学术术语库 + LLM翻译 |

**技术实现**：
- 对话引导：前后端状态机 + LLM 关键词提取
- 检索：复用 v1 的 OpenAlex API 调用
- 关键词优化：LLM 生成 + 本地规则库双重保证

---

### 模块B：文献综述生成与可视化

**定位**：从文献列表到综述草稿的一步到位。

**流程**：

```
1. 用户从模块A检索结果中勾选待分析文献（或全选）
2. 系统批量获取摘要、关键词、年份、引用量
3. LLM 分析 → 识别：
   - 研究主题聚类（2-5个研究方向）
   - 时间线脉络（各阶段代表文献）
   - 方法演进（从传统到前沿）
   - 争议焦点 / 研究空白
4. 生成综述文本：
   - 研究背景 → 国内外现状 → 各分支梳理 → 研究空白 → 本文定位
5. 生成可视化图表（ECharts）：
   - 年发文量趋势折线图
   - 关键词共现网络图
   - 文献时间线气泡图
   - 研究主题占比饼图/桑基图
6. 用户预览、编辑、导出
```

**核心功能**：

| 功能 | 说明 |
|------|------|
| 批量文献分析 | 对勾选的 N 篇文献批量获取摘要和元数据 |
| 主题聚类 | LLM 自动识别 2-5 个研究方向 |
| 综述文本生成 | 结构化 LLM prompt，输出 Markdown 格式综述 |
| 趋势图 | 年发文量折线图 |
| 共现网络 | 高频关键词共现关系可视化 |
| 时间线气泡 | X=年份, Y=主题, 气泡大小=引用量 |
| 综述导出 | Markdown / Word 格式 |

**技术实现**：
- 摘要获取：OpenAlex API（abstract_inverted_index → 还原文本）
- LLM 综述：构建结构化 prompt → 调用 LLM API → 解析返回
- 可视化：前端 ECharts，后端返回结构化 JSON（节点/边/时序数据）

---

### 模块C：数据分析与结果解读

**定位**：论文后半段——跑数据、看结果、写解读。**结果解析对用户完全透明**：选好变量点执行，页面直接出表格和解读。

**流程**：

```
1. 用户上传数据文件（CSV/Excel）
2. 系统引导选择分析方法（下拉菜单，支持全部 Stata 命令）
3. 变量配置面板：Y（可多选）/ X（可多选）/ M（可多选）/ W（可多选）/ controls
4. 单模型模式：
   a. 系统自动生成 do 文件
   b. 自动通过 PowerShell 调用 Windows Stata/MP 18 执行（方案B，无须确认）
   c. 保留完整 log 文件和 do 文件供查阅
   d. 系统自动解析结果 → 页面直显表格
   e. LLM 自动生成 3 段解读 + 创新点提炼
5. 排列组合遍历模式（新增核心功能）：
   a. 用户勾选多个 Y/X/M/W
   b. 系统自动枚举所有排列组合
   c. 批量生成并执行 N 个 do 文件
   d. 汇总结果对比表（R² / 显著性 / AIC / BIC 排序）
   e. 自动推荐 Top N 组合并给出推荐理由
6. 生成论文"实证结果与分析"章节草稿（含表格 + 解读文本）
```

**核心功能**：

| 功能 | 说明 |
|------|------|
| 数据预览 | CSV/Excel 前 20 行预览，字段类型检测 |
| 方法选择面板 | 下拉选择分析方法，支持全部 Stata 命令（reg/reghdfe/xtreg/ivreg2/probit/logit/tobit/heckman/mediate/diff/didregress/eventdd/staggered/csdid/psmatch2/boottest 等） |
| 多变量配置 | Y / X / M / W / controls 均支持多选 |
| 排列组合遍历 | 自动枚举所有组合，批量执行 Stata，生成对比排名表 |
| Stata 模板生成 | 根据配置自动生成 do 文件，执行后保留 |
| 中文路径处理 | 上传文件自动复制到英文临时目录，避免 Stata 崩溃 |
| Stata 批处理 | 通过 PowerShell 调用 Windows Stata/MP 18，纯批处理自动执行 |
| 结果解析 | 底层自动解析，对用户完全透明，页面直显表格 |
| LLM 结果解读 | 自动生成3段式解读：显著性 → 经济含义 → 创新点 |
| 章节草稿生成 | "实证结果与分析" + "稳健性检验" + "进一步分析" |
| 导出 | Markdown 格式，可粘贴到论文中 |

**技术实现**：
- 数据预览：后端 pandas 读取前 20 行
- Stata 模板：预编写 20+ 种命令对应的 do 文件模板 + 变量名替换
- Stata 执行：复用已有 `stata-empirical-workflow` skill 的 PowerShell 桥接，复制到 C:\temp\stata_work\ 执行
- 结果解析：正则解析 Stata log 文件，按命令类型分格式解析（reg/reghdfe/xtreg/ivreg2 等各有一套解析模板）
- LLM 解读：将回归表模板化后喂给 LLM
- 排列组合：后端 Python 生成笛卡尔积组合列表 → 批量生成 do 文件 → 串行执行 Stata → 汇总解析

---

## 四、分阶段交付计划

### P0 — 对话式智能检索（预计 1-2 天）

**目标**：在 v1 基础上增加对话引导 + 关键词优化，交付可用检索工具。

**交付物**：
- [ ] 对话引导 UI（消息式交互，3-4轮对话）
- [ ] LLM 关键词提取与扩写
- [ ] 检索式自动生成
- [ ] 多库切换（OpenAlex 主 + WoS 镜像）
- [ ] 文献列表 + 引文导出 + 翻译（复用 v1）

### P1 — 文献综述生成与可视化（预计 3-5 天）

**目标**：从检索结果一键生成文献综述草稿 + 可视化图表。

**交付物**：
- [ ] 批量文献分析（摘要获取 + 元数据聚合）
- [ ] LLM 综述生成（结构化 prompt）
- [ ] ECharts 可视化（趋势图、共现网络、时间线气泡、主题桑基）
- [ ] 综述预览与编辑界面
- [ ] 综述导出（Markdown）

### P2 — 数据分析与结果解读（预计 3-4 天）

**目标**：数据上传 → Stata 自动执行 → 结果解读的完整链路 + 排列组合遍历。

**交付物**：
- [ ] 数据上传 + 预览
- [ ] 分析方法选择面板（支持全部 Stata 命令）
- [ ] 多变量配置（Y/X/M/W/controls 多选）
- [ ] Stata do 文件模板生成（20+ 命令模板）
- [ ] 中文路径处理（自动复制到英文临时目录）
- [ ] Stata 自动批处理调用（PowerShell 桥接，方案B自动执行）
- [ ] 运行日志 + do 文件保留下载
- [ ] 回归结果解析（对用户透明，页面直显表格）
- [ ] 排列组合遍历 + 对比排名表
- [ ] LLM 结果解读 + 创新点提炼
- [ ] 论文章节草稿导出

---

## 五、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| OpenAlex 速率限制 | 中 | 高 | 添加邮箱认证 + 本地缓存 |
| WoS API 需要机构订阅 | 高 | 中 | 优先 OpenAlex，WoS 做引导跳转 |
| LLM 综述质量不稳定 | 中 | 中 | 结构化 prompt + 后处理规则 |
| Stata 从 WSL 调用不稳定 | 中 | 高 | 复用已有 skill 中的稳定方案（PowerShell桥接+英文临时目录） |
| 排列组合遍历组合数过大 | 中 | 中 | 限制最多 3Y×5X×3M×3W = 135 种组合，超出提示用户分批 |
| 中文翻译质量 | 中 | 低 | 术语库兜底 + 可配置专业翻译 API |

---

## 六、依赖与前提

| 依赖 | 类型 | 说明 |
|------|------|------|
| Python 3.10+ | 运行环境 | FastAPI 后端 |
| pip 依赖 | 运行环境 | fastapi, uvicorn, httpx, pydantic |
| Stata/MP 18 | 必要（P2） | 安装在 Windows，通过 PowerShell 桥接调用 |
| Stata ssc 包 | 必要（P2） | reghdfe, ivreg2, outreg2/esttab, psmatch2, mediate, sgmediation, diff, boottest, csdid, eventdd 等 |
| LLM API Key | 可选 | 模块B综述生成 + 模块C结果解读（Hermes / GLM / DeepSeek） |
| 网络 | 运行环境 | 需访问 OpenAlex API |

---

## 七、迭代策略

```
P0（1-2天） → 演示版 → 反馈 → P1（3-5天） → 演示版 → 反馈 → P2（2-3天）
```

每个阶段交付可独立演示的版本，不依赖后续阶段。
P0 是最小可行产品，P1/P2 是增值模块。
