# PaperAgent 2.0 — 交付边界文档

> 逐项明确每个阶段「做什么」和「不做什么」 · v0.1 · 2026-07-07

---

## 通用原则

| 维度 | 交付边界 |
|------|---------|
| **部署方式** | 本地运行（Python FastAPI + 浏览器访问），不提供云部署 |
| **数据持久化** | 不建设用户系统、不持久化存储文献数据，每次检索为独立会话 |
| **用户量** | 单用户本地使用，不支持多用户并发 |
| **学术规范** | 生成的综述草稿需用户自行核对引用准确性，不承担学术不端责任 |

---

## P0：对话式智能检索

### ✅ 交付范围

| 项目 | 说明 |
|------|------|
| 对话引导界面 | 消息式 UI，支持 3-4 轮多轮交互 |
| 研究方向引导问题 | 内置 5 种预设引导路径（经济管理/计算机/工程/医学/综合） |
| 关键词提取 | 从用户自然语言描述中提取 3-8 个核心学术关键词 |
| 关键词扩写 | 同义词 + 上下位词 + 相关词，输出 5-15 个扩展词 |
| 检索式生成 | 自动组装 AND/OR 逻辑检索式，支持用户手动编辑 |
| OpenAlex 检索 | 关键词 + 年份筛选 + 排序 |
| 数据库切换 | 支持 OpenAlex / WoS / CNKI / 万方 / 维普 按钮，非 OpenAlex 数据库自动引导 |
| 年份筛选 | 近五年 / 近十年 / 自定义时间区间 |
| 文献卡片展示 | 标题（可点击跳转DOI）、作者、期刊、年份、引用量、分区标签、类型标签 |
| 单篇引文生成 | GB/T 7714 格式，一键复制 |
| 批量引文导出 | 勾选多篇 → 导出弹窗 → 一键复制全部 |
| 摘要展示 | 点击展开/收起 |
| 摘要翻译 | 内置学术术语库翻译，标注匹配的术语 |
| 智能扩词重试 | 结果为空时自动扩展关键词重新检索 |
| 无结果提示 | 显示检索优化建议（换词、扩范围、换库） |

### ❌ 不交付范围

- 全文下载（仅提供 DOI 链接和 OA 链接）
- 文献批量导出到文献管理工具（EndNote/Zotero 格式）
- 用户登录 / 收藏夹 / 阅读历史
- 本地文献缓存
- 知网/WoS/万方的直接 API 对接（仅做引导跳转）
- PDF 全文解析

---

## P1：文献综述生成与可视化

### ✅ 交付范围

| 项目 | 说明 |
|------|------|
| 批量文献选择 | 从检索结果勾选 N 篇（上限 50 篇）进行分析 |
| 摘要批量获取 | 自动从 OpenAlex 获取每篇文献的完整摘要 |
| 研究主题聚类 | LLM 识别 2-5 个研究方向，每方向列出代表文献 |
| 时间线脉络 | 按年份梳理各研究阶段的特点和标志性文献 |
| 方法演进分析 | 识别该领域方法的演变路径（传统 → 前沿） |
| 综述文本生成 | 5 段式结构：背景 → 国内外现状 → 分支梳理 → 研究空白 → 本文定位 |
| 年发文量趋势图 | ECharts 折线图，X=年份，Y=发文量 |
| 关键词共现网络 | ECharts 力导向图，节点=关键词，边=共现关系，节点大小=频次 |
| 文献时间线气泡图 | ECharts 气泡图，X=年份，Y=主题，气泡大小=引用量 |
| 研究主题桑基图 | ECharts 桑基图，年份→主题→高频关键词 流量 |
| 综述预览/编辑 | 文本预览区域，支持手动修改 |
| 综述导出 | Markdown 格式文件下载 |

### ❌ 不交付范围

- 自动引用编号与参考文献列表的实时同步
- 与 LaTeX 集成
- 文献质量评估（系统性偏倚风险评估）
- Meta 分析
- 多语言综述生成
- 综述查重
- 图表样式深度定制（提供 4 种图表，不提供自定义配色/布局）

---

## P2：数据分析与结果解读

### ✅ 交付范围

| 项目 | 说明 |
|------|------|
| 数据文件上传 | 支持 CSV / Excel（.xlsx）格式，单文件 ≤ 50MB |
| 数据预览 | 前 20 行表格预览，字段名、类型自动检测 |
| 描述性统计 | 自动生成描述性统计表（obs, mean, sd, min, max） |
| 相关性矩阵 | 核心变量间相关系数矩阵 |
| 方法选择面板 | 下拉选择分析方法；支持完整 Stata 命令集 |
| 变量配置 | 从数据字段中下拉选择 Y / X / M / W / controls |
| Stata do 文件生成 | 根据配置自动生成可执行的 do 文件，执行后保留原始 do 文件 |
| Stata 远程调用 | 通过 PowerShell 桥接调用 Windows Stata/MP 18，纯批处理模式（无 GUI） |
| 中文路径处理 | 用户上传文件自动复制到英文临时目录执行，避免 Stata 中文路径崩溃 |
| 回归结果解析 | 底层自动解析 Stata log，提取系数/标准误/p 值/R²/N，**对用户完全透明，无需理解** |
| 结果表格生成 | 页面直显规范三线表（含 ***/**/* 显著性标注） |
| 全程自动执行 | 配置后自动执行，无须用户确认（方案B） |
| 运行日志保留 | 每次执行保留完整的 Stata log 文件，可下载查阅 |
| LLM 结果解读 | 3 段式自动生成：核心发现 → 经济显著性 → 与文献对话 |
| 创新点提炼 | LLM 基于结果识别创新点（理论/方法/视角/数据） |
| 论文章节草稿 | "实证结果与分析" + "稳健性检验" + "进一步分析" 三章节 Markdown |
| 导出 | Markdown 文件下载，可直接粘贴到论文中 |

#### 支持的 Stata 命令集（全部覆盖，免后续追加）

```
□ reg            — 普通 OLS 回归
□ reghdfe        — 高维固定效应（需 ssc install）
□ xtreg/xtset    — 面板固定效应/随机效应
□ xtnbreg        — 面板负二项回归
□ ivreg2/ivregress — 工具变量回归
□ probit/logit   — 二元选择模型
□ xtprobit/xtlogit — 面板二元选择模型
□ tobit          — 截断/归并回归
□ heckman        — Heckman 选择模型
□ mediate        — 中介效应
□ sgmediation    — Sobel-Goodman 中介检验
□ diff           — 双重差分（DID）
□ didregress     — Stata 18 内置 DID
□ eventdd        — 事件研究法 / 动态 DID
□ staggered      — 交错 DID（Callaway & Sant'Anna）
□ csdid          — 多时点 DID
□ psmatch2/teffects — 倾向得分匹配
□ regdic         — 断点回归（RDD）
□ boottest       — 自助法检验
□ outreg2/esttab — 结果导出
□ estout         — 结果整理
```

#### 排列组合遍历功能

| 项目 | 说明 |
|------|------|
| 多变量勾选 | Y / X / M / W 各支持多选 |
| 自动遍历 | 勾选后自动生成所有排列组合，批量执行 Stata |
| 筛选排序 | 按 R² / 核心变量显著性 / AIC / BIC 排名 |
| 结果展示 | Top N 组合对比表（排名+模型公式+系数+R²+p值+推荐理由） |
| 最优推荐 | 自动标注最优模型并给出推荐理由 |
| do 文件保留 | 每次遍历的 do 文件全部保留，可复查 |

### ❌ 不交付范围

- 面板数据清洗（不自动做缩尾/插值/异常值剔除，生成 do 文件由用户确认后执行）
- 空间计量 / 贝叶斯 / 纯机器学习方法
- 问卷数据分析（结构方程模型等）
- 用户上传数据的隐私存储（处理完后不保留原始数据）
- Stata 以外的计量软件支持（R/Python/SPSS）
- 图形化回归结果（系数图、边际效应图等）
- 结果显著性的 p-hacking 检测
- 自动选择最优模型（遍历结果展示给用户，由用户最终决策）

---

## 跨阶段不交付清单

以下功能在所有阶段均**不提供**：

- ❌ 云服务 / SaaS 部署
- ❌ 多用户 / 权限系统
- ❌ 移动端 App
- ❌ 浏览器插件
- ❌ AI 自动写全篇论文（仅辅助生成章节草稿）
- ❌ 数据爬取（不爬知网/万方/维普的全文）
- ❌ 论文排版 / LaTeX 编译
- ❌ 期刊推荐 / 投稿辅助
- ❌ 查重 / AI 检测规避
