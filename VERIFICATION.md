# PaperAgent 2.0 — 功能校验文档

> 逐项验收标准 + 测试用例 · v0.1 · 2026-07-07

---

## 使用说明

- **状态标记**：✅ 通过 / ❌ 未通过 / ⏳ 待测
- **测试环境**：WSL (Ubuntu) + Python 3.12 + 浏览器（Chromium）
- **前置条件**：`uvicorn app:app` 服务运行在 `http://localhost:8080`

---

## P0：对话式智能检索

### A-01 页面加载

| 项目 | 验收标准 |
|------|---------|
| 页面可访问 | 浏览器打开 `http://localhost:8080` 正确加载 HTML |
| 无 JS 错误 | 浏览器控制台无红色报错 |
| 外部资源 | Font Awesome 图标正常加载 |
| 响应式 | 缩小至 375px 宽度，布局不崩、按钮不重叠 |

**测试用例**：
```
1. 打开 http://localhost:8080
2. 按 F12 → Console 查看是否有错误
3. 调整浏览器宽度至 375px 检查布局
4. 检查图标（检索按钮、数据库图标等）是否正常显示
```

---

### A-02 对话引导

| 项目 | 验收标准 |
|------|---------|
| 引导入口 | 页面加载后显示对话引导区（非空搜索框） |
| 首轮提问 | 显示"你想研究什么方向？"消息 |
| 用户输入响应 | 输入研究方向后，系统追问细化问题 |
| 引导路径 | 至少 5 种预设路径（经济学/计算机/医学/工程/综合） |
| 关键词推荐 | 对话结束后输出 3-8 个推荐关键词 |

**测试用例**：
```
输入："我想研究企业数字化转型"
预期：
- 追问 "具体关注因果效应还是测度构建？"
- 追问 "是否限定特定行业？"
- 输出关键词：digital transformation, 数字化转型, firm performance, IT capability, 企业创新
```

---

### A-03 关键词检索

| 项目 | 验收标准 |
|------|---------|
| 基本检索 | 输入单个关键词，返回 ≥1 条结果 |
| 多词检索 | 输入"machine learning healthcare"，返回相关结果 |
| 中文检索 | 输入"深度学习"，返回中文/英文相关文献 |
| 检索耗时 | 首次检索 ≤ 10 秒（OpenAlex API 响应时间） |
| 无结果处理 | 无结果时显示优化建议 + 自动扩词重试 |

**测试用例**：
```
TC-01: keyword="deep learning", 预期返回 ≥1 条结果
TC-02: keyword="企业数字化转型", 预期返回 ≥1 条结果
TC-03: keyword="zzzxxxnonexistent", 预期显示"未找到" + 扩词建议
```

---

### A-04 年份筛选

| 项目 | 验收标准 |
|------|---------|
| 近五年 | 勾选后结果年份 ≥ 当前年份-5 |
| 近十年 | 勾选后结果年份 ≥ 当前年份-10 |
| 自定义区间 | 输入 2018-2022，结果仅包含该区间文献 |
| 不限 | 显示所有年份结果 |
| 切换年份 | 切换年份后重新检索 |

**测试用例**：
```
TC-01: 搜索"deep learning" + 近五年 → 检查结果年份 ≥ 2021
TC-02: 自定义 2010-2012 → 检查所有结果在 2010-2012 范围内
TC-03: 点击"不限" → 结果包含各年份文献
```

---

### A-05 数据库切换

| 项目 | 验收标准 |
|------|---------|
| OpenAlex 激活 | 默认选中，搜索使用 OpenAlex API |
| 知网按钮 | 点击后提示"需校园网访问" + 自动用 OpenAlex 镜像检索 |
| WoS 按钮 | 同知网逻辑 |
| 万方按钮 | 同知网逻辑 |
| 维普按钮 | 同知网逻辑 |
| 切换回 OpenAlex | 点击后恢复正常 OpenAlex 检索 |

**测试用例**：
```
TC-01: 默认状态 → 搜索 → 结果来自 OpenAlex
TC-02: 点击"中国知网" → 显示引导提示 → 自动转 OpenAlex 检索
TC-03: 知网搜索后点击"开放学术" → 正常检索
```

---

### A-06 文献卡片

| 项目 | 验收标准 |
|------|---------|
| 卡片数量 | 每次返回 ≤ 20 条，滚动加载或分页 |
| 标题显示 | 完整显示，可点击跳转 DOI 链接 |
| 作者显示 | 显示前 5 位作者 + "等"（超过 5 位时） |
| 期刊/来源 | 显示期刊名称或来源类型 |
| 年份 | 显示出版年份 |
| 引用量 | 显示被引次数 |
| 分区标签 | 显示 Q1/Q2/Q3/Q4 + "高被引"/"较高引用" 等 |
| 类型标签 | 显示期刊论文/综述/学位论文等 |
| OA 标签 | 开放获取文献显示"开放获取"绿色标签 |

**测试用例**：
```
TC-01: 搜索后检查第一条卡片的完整性
       ✓ 标题可点击 ✓ 有作者 ✓ 有期刊 ✓ 有年份 ✓ 有引用量 ✓ 有分区
```

---

### A-07 引文生成

| 项目 | 验收标准 |
|------|---------|
| 点击"引文" | 生成并显示 GB/T 7714 格式引文 |
| 格式规范 | `作者. 题名[文献类型]. 期刊, 年份. DOI: xxx.` |
| 作者格式 | 1-3 人全列，超过 3 人列前 3 + "等" |
| 一键复制 | 点击"复制"按钮后引文进入剪贴板 |
| 复制反馈 | 按钮文字变为"已复制" + 绿色提示 |

**测试用例**：
```
TC-01: 点击首篇文献的"引文"按钮
       预期显示如：
       "Matthew J. Page, Joanne E. McKenzie, Patrick M. Bossuyt, 等.
        The PRISMA 2020 statement: an updated guideline for reporting
        systematic reviews[J]. BMJ, 2021, 372: n71. DOI: 10.1136/bmj.n71."
TC-02: 点击"复制"按钮 → 检查剪贴板内容
TC-03: 按钮变为"已复制"
```

---

### A-08 批量引文导出

| 项目 | 验收标准 |
|------|---------|
| 文献勾选 | 每张卡片左侧有 checkbox，勾选后卡片高亮 |
| 选中计数 | 顶部"导出选中(N)"实时更新 |
| 批量导出 | 勾选 N 篇后点击"导出选中" → 弹出引文列表 |
| 全部导出 | 点击"导出全部引文" → 弹出当前页全部引文 |
| 一键复制全部 | 弹窗内"一键复制全部" → 复制所有引文 |
| 弹窗关闭 | 点击 × 关闭弹窗 |

**测试用例**：
```
TC-01: 勾选第 1、3、5 篇 → "导出选中(3)" 可点击
TC-02: 点击"导出选中" → 弹窗显示 3 条引文
TC-03: 点击"一键复制全部" → 剪贴板包含 3 条引文
TC-04: 点击"导出全部引文" → 弹窗显示全部 20 条引文
```

---

### A-09 摘要展示

| 项目 | 验收标准 |
|------|---------|
| 点击"摘要" | 展开显示文献摘要 |
| 再次点击 | 收起摘要 |
| 无摘要文献 | 不显示"摘要"按钮 |

**测试用例**：
```
TC-01: 点击有摘要文献的"摘要" → 展开摘要内容
TC-02: 再次点击 → 收起
TC-03: 检查摘要内容与文献匹配
```

---

### A-10 学术翻译

| 项目 | 验收标准 |
|------|---------|
| 点击"翻译" | 在摘要下方显示翻译结果 |
| 术语高亮 | 匹配到的学术术语显示蓝色标签（如`神经网络`） |
| 翻译方向 | 英文→中文翻译专业术语 |
| 已翻译标记 | 再次点击不重复请求 |

**测试用例**：
```
TC-01: 展开摘要 → 点击"翻译"
       预期：显示翻译区域，包含"学术翻译"标题
TC-02: 检查是否显示术语标签（如 "neural network → 神经网络"）
TC-03: 再次点击"翻译" → 不重新请求 API
```

---

### A-11 无结果处理

| 项目 | 验收标准 |
|------|---------|
| 无结果显示 | 显示"未找到相关文献"提示 |
| 优化建议 | 显示具体的检索优化建议 |
| 扩词建议 | 推荐 3-5 个扩展关键词 |
| 扩词来源 | 扩词建议来自 LLM 或本地规则库 |

**测试用例**：
```
TC-01: 搜索极生僻词 → 显示"未找到" + 建议
TC-02: 建议中包含可点击的扩展关键词
```

---

## P1：文献综述生成与可视化

### B-01 批量文献分析

| 项目 | 验收标准 |
|------|---------|
| 批量选择 | 支持勾选 ≥2 篇文献（上限 50 篇） |
| 分析按钮 | 选中后"文献分析"按钮可用 |
| 摘要获取 | 自动获取每篇选中文献的完整摘要 |
| 处理中状态 | 显示进度条或加载动画 |
| 处理完成 | 显示分析结果概述（主题数、文献分布） |

**测试用例**：
```
TC-01: 勾选 10 篇文献 → 点击"文献分析"
       预期：显示加载 → 完成后显示"识别到 3 个研究方向"
```

### B-02 综述文本生成

| 项目 | 验收标准 |
|------|---------|
| 结构完整性 | 5 段：背景 → 国内外现状 → 分支梳理 → 研究空白 → 本文定位 |
| 文献引用 | 每段至少引用 2 篇选中文献 |
| 语言流畅 | 中文表达通顺，无明显翻译痕迹 |
| 可编辑 | 生成文本在可编辑区域显示 |

**测试用例**：
```
TC-01: 生成后检查文章结构是否符合 5 段式
TC-02: 检查每段是否引用所选文献
TC-03: 手动修改文本 → 修改生效
```

### B-03 可视化图表

| 项目 | 验收标准 |
|------|---------|
| 年发文量趋势图 | ECharts 折线图，X 轴年份，Y 轴发文数 |
| 关键词共现网络 | 力导向图，节点可拖拽，悬停显示关键词 |
| 文献时间线气泡 | 气泡图，颜色区分主题，大小代表引用量 |
| 主题桑基图 | 年份→主题→关键词 流量 |
| 图表交互 | 鼠标悬停显示详情 |
| 图表布局 | 四图在页面中合理排列，不重叠 |

**测试用例**：
```
TC-01: 检查趋势图 → X 轴年份递增，Y 轴合理
TC-02: 拖拽共现网络节点 → 节点移动、边跟随
TC-03: 悬停气泡 → 显示文献标题和引用量
TC-04: 缩小浏览器宽度至 768px → 图表纵向排列
```

### B-04 综述导出

| 项目 | 验收标准 |
|------|---------|
| 导出按钮 | 点击"导出综述"按钮 |
| 文件格式 | 生成 .md 文件下载 |
| 内容完整 | 包含综述全文 + 参考文献列表 |
| 参考文献格式 | 参考文献使用 GB/T 7714 格式 |

**测试用例**：
```
TC-01: 点击"导出综述" → 下载 .md 文件
TC-02: 打开文件 → 检查是否包含全文 + 参考文献
```

---

## P2：数据分析与结果解读

### C-01 数据上传与预览

| 项目 | 验收标准 |
|------|---------|
| 上传入口 | 页面有"上传数据"按钮或拖拽区 |
| 格式支持 | 支持 .csv 和 .xlsx，≤50MB |
| 预览表格 | 上传后显示前 20 行数据 |
| 字段检测 | 自动识别数值型/类别型字段 |
| 错误提示 | 格式不支持或超限时显示明确错误 |

**测试用例**：
```
TC-01: 上传 .csv 文件 → 显示前 20 行表格
TC-02: 上传 .xlsx 文件 → 正常解析
TC-03: 上传 .pdf 文件 → 显示"不支持的文件格式"
```

### C-02 方法选择与配置

| 项目 | 验收标准 |
|------|---------|
| 方法列表 | 下拉包含 OLS/固定效应/随机效应/DID/中介/调节 |
| 变量选择 | 从数据字段中下拉选择因变量、核心自变量、控制变量 |
| 必填校验 | 未选因变量/自变量时提示"请选择" |
| 参数配置 | 可配置聚类标准误、固定效应维度 |

**测试用例**：
```
TC-01: 上传数据 → 下拉显示数据中的所有字段名
TC-02: 选择 OLS → 配置因变量 Y、自变量 X、控制变量 controls
TC-03: 不选因变量点击"执行" → 提示"请选择因变量"
```

### C-03 Stata 执行

| 项目 | 验收标准 |
|------|---------|
| Do 文件生成 | 根据配置生成可执行的 do 文件 |
| 变量名正确 | do 文件中变量名与用户数据列名一致 |
| 调用 Stata | 通过 PowerShell 调用 Windows Stata/MP |
| 执行反馈 | 显示"正在执行"状态 |
| 完成通知 | 执行完成后显示"Stata 执行完成" |

**测试用例**：
```
TC-01: 配置 OLS 回归 → 检查生成的 do 文件语法正确
TC-02: 检查 do 文件中变量名 = 用户选择的数据列名
TC-03: 执行后检查 Stata log 文件是否存在
```

### C-04 结果解析

| 项目 | 验收标准 |
|------|---------|
| 系数提取 | 正确提取核心自变量回归系数 |
| 标准误提取 | 正确提取标准误 |
| 显著性标注 | 正确标注 *** p<0.01, ** p<0.05, * p<0.1 |
| R² 提取 | 正确提取 R²/调整 R² |
| 样本量提取 | 正确提取观测数 |
| 结果表格 | 以规范的三线表格式呈现 |

**测试用例**：
```
TC-01: 虚机测试：输入假回归结果 → 正确解析系数和 p 值
TC-02: 检查表格格式：系数在上，标准误在下，星号标注
```

### C-05 LLM 结果解读

| 项目 | 验收标准 |
|------|---------|
| 核心发现 | 首段说明核心系数方向、大小、显著性 |
| 经济显著性 | 第二段解释系数的实际含义（如 "X 每增加 1 个标准差，Y 增加 0.3 个标准差"） |
| 文献对话 | 第三段与已有文献对比（"该结果与 XXX(2020) 一致"） |
| 创新点 | 每段包含创新点标注（"理论创新：……""方法创新：……"） |
| 可编辑 | 生成文本支持用户修改 |

**测试用例**：
```
TC-01: 输入模拟回归结果 → 检查解读分 3 段
TC-02: 检查创新点是否被识别和标注
TC-03: 手动修改解读文本 → 修改生效
```

### C-06 论文章节导出

| 项目 | 验收标准 |
|------|---------|
| 章节生成 | 生成"实证结果与分析"+"稳健性检验"+"进一步分析"三章 |
| 内容填充 | 各章节包含相应分析结果表格和解读文本 |
| 格式 | Markdown 格式，含一级/二级标题 |
| 下载 | 点击"导出章节" → 下载 .md 文件 |

**测试用例**：
```
TC-01: 生成后检查是否包含三个章节标题
TC-02: 每个章节是否有表格 + 解读文本
TC-03: 导出检查 .md 文件内容完整
```

---

## 集成测试

### IT-01 端到端 P0（最短路径）

```
1. 打开浏览器 → 页面加载 ✅
2. 直接在搜索框输入 "deep learning"
3. 点击"检索"
4. 等待结果出现（≤15 秒）
5. 检查结果卡片 ≥1 条
6. 点击第一条的"引文" → 复制
7. 点击第一条的"摘要" → 展开
8. 点击"翻译" → 显示翻译
结果：完整链路走通
```

### IT-02 端到端 P0+P1

```
1. 搜索 "machine learning in healthcare"
2. 勾选 10 篇文献
3. 点击"文献分析"
4. 等待分析完成 → 检查综述文本
5. 检查 4 个图表是否渲染
6. 点击"导出综述" → 下载 .md
结果：P0→P1 链路走通
```

### IT-03 端到端 P0+P1+P2

```
1. 搜索 "digital transformation firm performance"
2. 勾选 5 篇文献 → 生成简要综述
3. 上传面板数据文件（.csv）
4. 选择 OLS 回归
5. 配置 Y/X/controls
6. 生成 do 文件 → 执行 Stata
7. 查看结果表格
8. 生成 LLM 解读
9. 导出论文章节
结果：全链路走通
```

---

## 性能要求

| 指标 | 标准 |
|------|------|
| 页面加载 | ≤ 3 秒（含 CDN 资源） |
| OpenAlex 检索 | ≤ 10 秒（含网络延迟） |
| LLM 综述生成 | ≤ 30 秒（50 篇文献内） |
| 图表渲染 | ≤ 2 秒（ECharts 初始化） |
| Stata 执行 | 取决于数据量，≤ 5 分钟 |
| 内存占用 | ≤ 512MB（后端进程） |

---

## 兼容性

| 浏览器 | 版本要求 |
|--------|---------|
| Chrome | ≥ 90 |
| Edge | ≥ 90 |
| Firefox | ≥ 90 |
| 移动端 | 不保证完整功能，基本布局可用 |
